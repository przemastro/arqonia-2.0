spring:
  data:
    mongodb:
      database: local
      port: 27017
      uri: mongodb://localhost


#TODO Remove after 'full OAUth2 implementation'!
management:
  endpoints:
    web:
      exposure:
        include: "*"

#ONLY FOR DEVELOPMENT AND TESTING !
logging:
  level:
     pl.astronomy.arqonia20: DEBUG
     ROOT: DEBUG

arqonia:
  admin:
    username: abekalarz
